# Add Homebrew to PATH if it's installed
if [ -x /opt/homebrew/bin/brew ]; then
    eval "$(/opt/homebrew/bin/brew shellenv)"
fi

export PATH="$HOME/.local/bin:$HOME/bin:$HOME/.local/share/npm:$HOME/.cargo/bin:$GOPATH/bin:/Applications/MATLAB_R2025b.app/bin:$PATH"
export EDITOR='nvim'
export MANPAGER="nvim +Man!"
